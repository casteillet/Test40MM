using UnityEngine;

public class AgentFactory
{
    public GameObject CreateAgentFromRecruit(/*Data data,*/ Transform parent)
    {
        // TODO : Reput in transform parent ?
        // var instance = Object.Instantiate(recruit.PresetData.BaseData.AgentPrefab, parent);
        // var instance = Object.Instantiate(recruit.PresetData.BaseData.AgentPrefab, parent.position, parent.rotation);
        // var agent = instance.GetComponent<Agent>();
        // agent.Initialize(recruit);

        // return instance;
        return null;
    }
}